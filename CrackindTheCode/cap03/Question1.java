package cap03;

public class Question1 {

	/*
	 * One possible way would be to separate by mod 3, but to improve the use of
	 * space when adding a new element we first check if this stack much more
	 * elements than other two, if so we could start to use linear, and when
	 * adding to the smaller stacks we could check if we are going to colide and
	 * then rearrange the stacks.
	 */
}
